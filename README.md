1. `uv sync`する
2. `python src/entry.py`で起動

起動すると、画面が2つ出てきます

片方はカメラ入力をトレースしており、ARuCoマーカを検出すると対象範囲を緑の枠で囲って表示します

同時にキャプチャ対象となる範囲のみが2つ目の画面に表示されます

マーカが認識された状態で`c`を押すと2つ目の画面に表示されている画像が固定され、`s`でサーバに送信できる状態になります

キャプチャを再度実行したい場合は`r`を押してください
